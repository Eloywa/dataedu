# Зависимости DataEdu. Ставятся одной командой: pip install -r requirements.txt
#
# Версии зафиксированы по нижней границе, а не намертво: проект должен собираться
# и через год, а обновления внутри мажорной версии Django совместимы.

django==5.2.*
markdown>=3.10.2
python-dotenv>=1.2.2

# Драйвер PostgreSQL. Нужен только при DB_ENGINE=postgres, но ставится всегда:
# иначе переключение на боевую СУБД упирается в отдельную установку.
psycopg[binary]>=3.3.4

# Отдача статики самим приложением. В контейнере статику забирает nginx, но
# WhiteNoise остаётся: он нужен и в разработке, и как запасной путь, если
# приложение поднимают без прокси.
whitenoise>=6.9

# Рабочий сервер для эксплуатации. runserver для этого не годится: он
# однопоточный, не переживает нагрузку и сам об этом предупреждает.
gunicorn>=23.0

# Оформление админки: через неё преподаватель создаёт курсы и задания.
django-jazzmin>=3.0.1

# --- Разработка --------------------------------------------------------------
# Проверка и форматирование кода: настройки — в pyproject.toml.
ruff>=0.16

# --- Только для эксплуатации на нескольких процессах -------------------------
# Общий кэш и сессии. Раскомментируйте, если задаёте REDIS_URL.
# redis>=5.0
